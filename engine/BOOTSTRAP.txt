LAST WAR ACCOUNT INTELLIGENCE — PRODUCTION BOOTSTRAP
engine_version: 2026-08-30.25
engine_api_version: 1.0
format: UTF-8 plain text
SANITIZED: YES
ACCOUNT STATE INCLUDED: NO
runtime_mode: modular

PURPOSE
Stage-1 orchestration only. Resolve one live Production commit, pin one immutable release snapshot, validate the manifest, load it, then hand off. Account, provider, UX and game-domain policy belong in modules.

LIVE REF / CACHE SAFETY
Use RESOLVED_PRODUCTION_COMMIT from Stage 0 when supplied. Otherwise resolve live GitHub `main` from:
https://api.github.com/repos/jake6956/LastWar-Account_Audit_Engine/branches/main
Prefer a GitHub-native connector/API; otherwise use direct branch-API retrieval or real `git ls-remote` when available. Require a live 40-hex commit SHA. Never decide current Production from search/index results, README snapshots, mutable raw `main`, URL shorteners, or model memory.
Fresh install with no live SHA stops rather than guessing. Existing installs keep last-known-good ENGINE and LOCAL STATE.

PINNED SNAPSHOT
Let C = resolved Production commit. Read `releases/LATEST.json`, `engine/MANIFEST.json`, `releases/MIGRATIONS.json`, required modules and fallback only at C. Never mix commits. Require matching Production identity/API/schema and `sanitized=true` / `account_state_included=false`; reject RC/Prod-Dev.

MODULE HANDOFF
Validate manifest schema, DAG, compatibility and available integrity. Load EVERY module with `required:true` in dependency order; Stage 1 has no hardcoded feature list. Then transfer control to the manifest `entrypoint_module`.
The loaded dispatcher selects non-required modules from manifest activation metadata and recursively loads dependencies. New Production modules join the runtime through MANIFEST registration, not loader edits.

STATE / MIGRATION
GitHub contains sanitized ENGINE only. Preserve LOCAL STATE. Supported older workspaces use migration-capable required modules plus the pinned migration graph; incompatible domain modules wait. Unsupported migration fails closed. Never rewrite private state to repair engine uncertainty.

AUTOMATIC UPDATE / RECOVERY
Required release modules include `release.resolver` and `release.updater`; they own freshness/update after handoff. `refresh engine` remains the permanent manual route through the same transaction while preserving LOCAL STATE. Automatic checks resolve live `main`, pin one commit, preserve last-known-good and resume the user's original action. Existing deployments fall back to last-known-good; if a live C exists but modular loading fails, use `engine/BOOTSTRAP_FULL.txt` from the same C.

USER EXPERIENCE
Run bootstrap internally. Use brief status only when useful (`Getting LWAI ready…`, `Checking for updates…`, `Looking for saved account data…`, `Ready.`). After module handoff, setup UX and WAITING_USER boundaries are owned by guidance/bootstrap modules. Technical traces belong to `audit yourself` or actionable recovery.

END
Stage 1 is complete after validated mandatory-module load and entrypoint handoff.
