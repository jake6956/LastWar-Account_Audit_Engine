LAST WAR AI — PUBLIC CONFIGURATION DELIVERY CONTRACT

SANITIZED: YES
ACCOUNT STATE INCLUDED: NO

PURPOSE
The public client should need only https://lastwarai.com for initial setup. Cloudflare resolves current GitHub Production server-side, retrieves the complete sanitized engine from that exact immutable commit, adapts only the obsolete Stage-0 discovery section for first-party delivery, and returns the complete configuration in the same HTTP response.

PLAYER UX
The public installer sentence remains:
Set up Last War optimization using the instructions at https://lastwarai.com

The user is not required to retrieve GitHub JSON, locate a commit SHA, copy a second URL, or perform technical setup. If a user opens or copies the LastWarAI.com response directly, the returned document is itself the complete public configuration.

ROOT RESPONSE CONTRACT
A successful HTTP 200 response from https://lastwarai.com must:
- be UTF-8 text/plain;
- include X-LWAI-Commit with the current 40-character lowercase hexadecimal GitHub Production SHA;
- include LAST WAR AI — PUBLIC CONFIGURATION;
- include the complete sanitized BOOTSTRAP_FULL engine from that same exact commit;
- state SANITIZED: YES and ACCOUNT STATE INCLUDED: NO;
- transparently identify the public GitHub source/provenance;
- explicitly permit independent verification when the host supports it;
- explicitly state that the configuration does not override the host AI platform's system, developer, security, privacy, or safety requirements;
- contain no second-stage AI fetch command or anti-verification instruction.

TRUST / VERSION AUTHORITY
GitHub main remains the underlying Production source and version authority. LastWarAI.com is first-party delivery infrastructure. Cloudflare performs live GitHub main resolution and exact-commit source retrieval server-side. Search results, cached README pages, mutable raw main, redirects, shorteners, or model memory do not establish Production.

FIRST-PARTY DELIVERY ADAPTATION
BOOTSTRAP_FULL.txt contains the generic/direct-source Stage-0 resolver needed for standalone recovery. When LastWarAI.com has already resolved Production server-side, the Worker replaces only that Stage-0 discovery section with a transparent statement that the source revision has already been established and may be independently verified. Gameplay, account, storage, evidence, continuity, update, and optimization behavior remain unchanged.

COMPATIBILITY ENDPOINT
GET /engine/<40-hex SHA> remains available for backwards compatibility with the prior same-domain engine-proxy transport. It returns the complete sanitized engine from that exact GitHub commit, includes X-LWAI-Commit with the same SHA, and uses immutable caching. New installs do not require this second request.

ROBOTS / DISCOVERABILITY
GET /robots.txt explicitly allows OAI-SearchBot, ChatGPT-User, and general crawlers.

FAILURE
If Cloudflare cannot establish a valid live GitHub SHA or retrieve/validate the exact Production engine, the root fails closed with HTTP 503 and a plain-language retry message. Never guess a commit and never return partial or unrelated engine content.
